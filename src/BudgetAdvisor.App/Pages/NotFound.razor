@inject LocalizationService Localizer

<PageTitle>Budget Advisor - @Localizer["common.notFound"]</PageTitle>

<LayoutView Layout="@typeof(MainLayout)">
    <section class="placeholder-panel">
        <h1>@Localizer["common.notFound"]</h1>
        <p role="alert">@Localizer["common.notFoundMessage"]</p>
    </section>
</LayoutView>
